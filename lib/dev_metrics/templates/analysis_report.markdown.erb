# 📊 Development Summary Report

**Repository**: <%= summary[:repository_info] ? summary[:repository_info][:name] : 'Unknown' %>  
**Analysis Date**: <%= summary[:repository_info] ? summary[:repository_info][:analyzed_at] : Time.now %>

<% if summary[:contributor_filter] %>
> **👥 Contributors**: <%= summary[:contributor_filter][:contributors].join(', ') %> (<%= summary[:contributor_filter][:count] %> total)

<% end %>
## 📈 Quick Stats

- **Total Metrics**: <%= summary[:total_metrics] || 0 %>
- **Execution Time**: <%= format_execution_time(summary[:execution_time]) %>
- **Data Coverage**: <%= summary[:data_coverage] || 0 %>%

<% categories = group_analysis_results(results) %>
<% categories.each do |category, metrics| %>
## 🔍 <%= category.to_s.upcase.gsub('_', ' ') %>

<% metrics.each do |metric_name, data| %>
<% metric_result = data[:metric] %>
### <%= metric_name.to_s.gsub('_', ' ').split.map(&:capitalize).join(' ') %>

- **Status**: ✅ Success
- **Data Points**: <%= metric_result.metadata[:data_points] || 0 %> <%= metric_result.metadata[:data_points_label] || 'records' %>
- **Value**: <%= format_metric_value(metric_result.value) %>
- **Execution Time**: <%= format_execution_time(data[:metadata][:execution_time]) %>

<% end %>
<% end %>

---
*Generated by DevMetrics CLI*