#!/usr/bin/env ruby

require_relative '../lib/dev_metrics'

begin
  DevMetrics::CLI::Runner.new(ARGV).run
rescue Interrupt
  puts "\nInterrupted by user"
  exit 1
rescue => e
  puts "Error: #{e.message}"
  exit 1
end